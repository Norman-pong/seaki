[package]
name = "seaki-daemon"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
seaki-core = { path = "../seaki-core" }

[dev-dependencies]
seaki-index = { path = "../seaki-index" }
