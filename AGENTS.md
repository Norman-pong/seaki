# AGENTS.md

本文件约束当前目录及其所有子目录中的 agent 工作方式。

## 语言

- 面向用户的回复默认使用简体中文。
- 文档新增内容默认使用简体中文，除非原文件或术语上下文需要英文。

## 基本约定

- 先查 wiki，再改代码；不要凭记忆改播放链路、包产物或发布规则。
- 变更保持小步、可测试、可回滚；不要提交 `dist/`、`target/` 等生成物。
- 每个阶段按“编码 -> 测试 -> 审阅 -> 修复 -> 提交”闭环；提交信息使用中文 Lore commit。

## 架构文档

- [docs/architecture.md](docs/architecture.md) 是本项目的架构文档入口，也是架构 wiki 索引页。
- 开始涉及架构、模块边界、权限、安全、数据生命周期、前端契约、插件、管道或 MVP 范围的工作前，先阅读 `docs/architecture.md`，再按索引进入对应主题页。
- 架构事实应维护在 `docs/architecture/` 下的权威主题页；`docs/architecture.md` 只保留导航摘要、总图、阅读路线、主题页索引和参考溯源。
- 修改架构设计时，同步更新 `docs/architecture.md` 的相关入口、主题页表或阅读路线，并在 `docs/architecture/maintenance-log.md` 记录结构性调整。

## 工作原则

- 保持改动小而明确，优先复用现有文档结构和命名。
- 不把推测写成事实；如果代码、测试和文档不一致，先回查证据，再标记不确定性或修正文档。
- 不随意新增依赖、目录层级或抽象；除非它们解决当前任务的真实复杂度。
- 涉及安全边界、权限模型、source/wiki/citation 权威关系或沙盒执行链路时，优先遵循架构文档中的不变式和权威主题页。

## 验证

- 文档改动后至少检查本地 Markdown 链接是否仍可解析。
- 代码改动后按项目实际工具运行相关测试、类型检查或最小可行验证；若暂时无法验证，必须说明原因。
